<% @list.bookmarks.each do |bookmark| %>
  <div>
    <%= bookmark.movie.title %> - <%= bookmark.comment %>
    <%= link_to 'Delete Bookmark', list_bookmark_path(@list, bookmark), method: :delete, data: { confirm: 'Are you sure you want to delete this bookmark?' } %>
  </div>
<% end %>
