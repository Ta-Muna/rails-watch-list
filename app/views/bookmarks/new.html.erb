<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :movie_id, collection: Movie.all.map { |movie| [movie.title, movie.id] }, prompt: "Select a movie" %>
  <%= f.input :comment %>
  <%= f.submit 'Create Bookmark' %>
<% end %>

