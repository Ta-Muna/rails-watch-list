<h1>All Lists</h1>

<ul>
  <% @lists.each do |list| %>
  <h2><%= link_to list.name, list_path(list) %></h2>
  <%= link_to "Create a Bookmark for #{list.name}", new_list_bookmark_path(list) %>
<% end %>
</ul>

<%= link_to 'Create a New List', new_list_path %> <!-- Link to create a new list -->

