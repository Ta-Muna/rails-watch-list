<h1>All Lists</h1>

<ul>
  <% @lists.each do |list| %>
  <li>
  <h2>  <%=link_to "📋 #{list.name} 📋", list_path(list)%> </h2>
  <br>
  <%= link_to "Create a Bookmark for  #{list.name}", new_list_bookmark_path(list), class: "link"%>
or
<%= link_to 'Delete', list_path(list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger"%>

</li>
<% end %>

</ul>

<%= link_to 'Create a New List', new_list_path, class: "btn btn-success"%><!-- Link to create a new list -->
