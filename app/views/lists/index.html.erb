<h1>All Lists</h1>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %> <!-- Link to view a single list -->
    </li>
  <% end %>
</ul>

<%= link_to 'Create a New List', new_list_path %> <!-- Link to create a new list -->
